#!/bin/sh
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
(cd "$ROOT/src/detonationLegacyThermophysicalTransportModels/legacyMixtureAverageFourier" && wclean libso || true)
(cd "$ROOT/applications/modules/detonationFluid" && wclean libso || true)
