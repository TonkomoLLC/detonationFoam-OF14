#!/bin/sh
set -eu
HERE=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
[ "${WM_PROJECT_VERSION:-}" = "14" ] || { echo "ERROR: source OpenFOAM Foundation v14 first" >&2; exit 2; }
wmake libso "$HERE"
echo "Built: $FOAM_USER_LIBBIN/libplanarFvMeshTopoChangers.so"
